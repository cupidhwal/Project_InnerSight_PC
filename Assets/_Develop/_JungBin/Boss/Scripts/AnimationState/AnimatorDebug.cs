using UnityEditor.Animations;
using UnityEngine;

public class AnimatorDebug : MonoBehaviour
{
    public Animator animator;

 /*   void Start()
    {
        foreach (AnimatorControllerParameter parameter in animator.parameters)
        {
            Debug.Log($"Animator Parameter: {parameter.name}");
        }

        foreach (var state in animator.Animations.GetBehaviours<AnimatorState>())
        {
            Debug.Log($"Animator State: {state.name}");
        }
    }*/
}
